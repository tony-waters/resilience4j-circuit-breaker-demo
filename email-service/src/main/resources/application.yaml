server:
  port: 8082

spring:
  application:
    name: email-service

demo:
  email-service:
    processing-delay: 250ms

management:
  endpoints:
    web:
      exposure:
        include: health,info
